import 'package:flutter/material.dart';

const Color appColor = Color(0xFF0091EA);
const TextStyle textStyle12 = TextStyle(fontSize: 12);
const TextStyle textStyle20 = TextStyle(fontSize: 20, color: Colors.white);

const Color unselected_nav_bar = Color(0xFFBDBDBD);
const EdgeInsets insets5 = EdgeInsets.all(5);
const Color main_background = Color(0XFFE8EAF6);
const EdgeInsets insets3 = EdgeInsets.all(3);
const EdgeInsets insets10 = EdgeInsets.all(10);
const EdgeInsets insets15 = EdgeInsets.all(10);
const EdgeInsets insets25 = EdgeInsets.all(25);
const EdgeInsets insets35 = EdgeInsets.all(35);
const EdgeInsets insets4 = EdgeInsets.all(4);
const EdgeInsets insetsLR5 = EdgeInsets.symmetric(horizontal: 5);
const EdgeInsets insetsLR10 = EdgeInsets.symmetric(horizontal: 10);
const BorderRadius radius10 = BorderRadius.all(Radius.circular(10));
const BorderRadius radius25 = BorderRadius.all(Radius.circular(25));
const BorderRadius radius50 = BorderRadius.all(Radius.circular(50));
const Offset offset_shadow = Offset(0, 0);
const double card_size = 100;
Color shadow = Colors.black.withOpacity(0.5);
Border border1 = Border.all(color: Colors.white, width: 1.0,);
